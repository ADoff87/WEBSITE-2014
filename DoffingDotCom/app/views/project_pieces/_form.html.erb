<%= form_for(@project_piece) do |f| %>
  <% if @project_piece.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@project_piece.errors.count, "error") %> prohibited this project_piece from being saved:</h2>

      <ul>
      <% @project_piece.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
